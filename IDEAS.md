# IDEAS

- Add history tracking to all views, make it easy to undo/redo

- Fix the list view, right now, reordering doesn't work. Use same logic as in Grid.

- Make dragging nodes onto other nodes cause them to swap in all modes.

